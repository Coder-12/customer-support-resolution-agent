# Optional. If not set, the agent uses a deterministic local draft generator.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Runtime configuration
APP_ENV=local
PROMPT_VERSION=resolution_agent_v1
CONFIDENCE_THRESHOLD=0.70
CHROMA_DIR=.chroma
COLLECTION_NAME=support_docs
VECTOR_STORE_MODE=auto
